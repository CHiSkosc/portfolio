title: Tina Young # your name (or website title) here
logo: "/images/headshot.jpg?raw=true" # your photo (or logo) here
description: > # your text below (remove <br> elements if you don't need line breaks)
  First description 
  <br><br>
  Second description 
  <br><br>
  <a href="https://www.linkedin.com/in/tina-young6925/">View My LinkedIn Profile</a> 
  
remote_theme: pages-themes/minimal@v0.2.0
plugins:
- jekyll-remote-theme # add this line to the plugins list if you already have one
- jekyll-feed # add this line to the plugins list if you already have one
- jekyll-seo-tag # add this line to the plugins list if you already have one
- jekyll-sitemap # add this line to the plugins list if you already have one
- jekyll-paginate # add this line to the plugins list if you already have one
- jekyll-archives # add this line to the plugins list if you already have one
- jekyll-assets # add this line to the plugins list if you already have one

gem "github-pages", group: :jekyll_plugins


